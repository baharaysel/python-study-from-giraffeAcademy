# Tuples are immutable we cant change  ( ) for tuple   [] for lists

coordinates = (4, 5)
print(coordinates[0])
#4

#coordinates[1] = 4
print(coordinates)
# it will give error as we cant change them

coordinates = [(4, 5), (6, 7), (80, 96)]
print(coordinates)
#[(4, 5), (6, 7), (80, 96)]   ()tuples are inside []the list. we cant change()tuples but we can add or remove them from []list

